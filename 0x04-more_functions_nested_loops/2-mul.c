#include <stdio.h>
#include <ctype.h>
#include "main.h"
/**
 * mul - A function that checks for uppercase character.
 * @a: An input character
 * @b: An input character
 * Return: x
 */
int mul(int a, int b)
{
int x;
x = a * b;
return (x);
}
