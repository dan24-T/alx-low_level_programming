#include "main"
/**
 * print_numbers - A function that checks for uppercase character.
 * @i: An input character
 * Return: 1 if i is uppercase or 0 otherwise
 */
void print_numbers(void)
{
int i;
for (i = '0'; i <= '9'; i++)
{
putchar(i);
}
putchar('\n);
return (0);
}
