#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
*_abs - entry point
* Description: function print_alphabet(void)
* @i: The character to print
* Return: Always (i) (Success)
*/
int _abs(int i)
{
i = abs(i);
return (i);
}
